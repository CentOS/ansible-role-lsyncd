# Defaults variables for role lsyncd

# List of local folders to watch and rsync to target, can be rsync:: url
# Not existing in default setup
# example:
# lsyncd_rsync_configs:
#   - name: test
#     source: /etc/
#     target: remote.host::rsync_module

# Increased based on your directory/file structure 
sysctl_fs_inotify_max_user_watches: '1524288'
